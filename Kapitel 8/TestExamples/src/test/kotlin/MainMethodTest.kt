package com.kmb.kotlin

import kotlin.test.assertEquals
import org.junit.Test
import org.junit.Rule
import org.junit.contrib.java.lang.system.SystemOutRule

class MainMethodTest {

/*    @get:Rule
    val systemOutput : SystemOutRule = SystemOutRule().enableLog()

    private val lineSeparator : String = System.lineSeparator()

    @Test
    fun testEmpty() {
        // Call main method to start our program
        main(emptyArray())

        // Capture system log
        val logOutput = systemOutput.log

        // Make the comparison against the expected result
        assertEquals("Bitte mindestens eine Zahl als Parameter angeben$lineSeparator", logOutput)
    }

    @Test fun testWrongParameter() {
        // Call main method to start our program
        main(arrayOf("Haus"))

        // Capture system log
        val logOutput = systemOutput.log

        // Make the comparison against the expected result
        assertEquals("Ganze Zahlen, bitte$lineSeparator", logOutput)
    }

    @Test
    fun testRegularParameter() {
        // Call main method to start our program
        main(arrayOf("3", "7", "${Int.MAX_VALUE}"))

        // Capture system log
        val logOutput = systemOutput.log

        // Make the comparison against the expected result
        assertEquals("9${lineSeparator}49${lineSeparator}Ich rechne nur bis 215${lineSeparator}", logOutput)
    }*/
}


